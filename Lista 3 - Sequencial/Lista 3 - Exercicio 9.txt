algoritmo "Lista 3 - Exercicio 9"

//Escreva um algoritmo para calcular as raízes de uma equação do segundo grau (Ax2 + Bx + C). Considere que o valor de A <> 0 e (B2 – 4AC) > 0. Escreva os valores na tela.

var
a, b, c, delta, x1, x2 : real

inicio
escreva ("Digite o valor de A: ")
leia (a)
escreva ("Digite o valor de B: ")
leia (b)
escreva ("Digite o valor de C: ")
leia (c)

se (a=0) entao
escreval ("Equação não possui raízes reais. O A tem que ser diferente de 0! ")
senao
delta <- b^2 -4*a*c
      se (delta>0) entao
      x1 <- (-b+delta^(1/2))/(2*a)
      x2 <- (-b-delta^(1/2))/(2*a)
      escreval ("O valor de x1 é: ",x1)
      escreval ("O valor de x2 é: ",x2)
      senao
           se (delta=0) entao
           x1 <- (-b)/(2*a)
           escreval ("A equação possui uma raiz real: ",x1)
           senao
           escreval ("A equação não possui raízes reais!")
           fimse
      fimse
fimse

fimalgoritmo