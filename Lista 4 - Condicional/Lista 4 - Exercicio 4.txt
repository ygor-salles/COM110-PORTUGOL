algoritmo "Lista 4 - Exercicio 4"

//Elabore um algoritmo que leia o nome da função de um funcionário e o valor do seu
//salário. Se a função for “Gerente”, verifique a regra abaixo.
//• Se salario < 5000, escreva “Reajuste de Salário”, caso contrário, escreva
//“Salário Compatível”
//Se a função não for gerente, escreva “Não é gerente”

var
cargo : caracter
salario : real

inicio
escreva ("Digite o seu cargo: ")
leia (cargo)
escreva ("Digite o seu salário: ")
leia (salario)

se (cargo <> "gerente") entao
escreval ("Não é gerente")
senao
     se (salario < 5000) entao
     escreval ("Reajuste de salario")
     senao
     escreval ("Salário Compatível")
     fimse
fimse

fimalgoritmo