algoritmo "Exercicio 2"

var
num, den, resultado : real

inicio
escreva ("Digite o valor do numerador: ")
leia (num)
escreva ("Digite o valor do denominador: ")
leia (den)

resultado <- num/den

escreval ("O valor do denominador é de: ",resultado)

fimalgoritmo